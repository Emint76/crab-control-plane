name: live-wrapper-execution-owner-validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  live-wrapper-execution-owner-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Phase 2 requirements
        run: pip install -r operations/harness-phase2/requirements.txt

      - name: Run live wrapper execution-owner test
        run: make live-wrapper-execution-owner-ci
