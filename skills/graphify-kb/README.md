# Graphify KB Skill Package

`skills/graphify-kb` in this repository is the canonical source for the
Graphify KB operational skill.

The runtime installation path is:

```text
/home/node/.openclaw/workspace/skills/graphify-kb
```

Direct edits to the runtime copy are forbidden. Runtime copies must be
synchronized from this repository version.

This package is operational only. It does not authorize normalization, entity
resolution, alias decisions, or semantic changes to Formulation or reference
data.
